import 'package:flutter/material.dart';

/// Reusable application-wide widget placeholder.
///
/// Add shared UI components here (e.g., custom buttons, loading
/// indicators, error displays) that are used across multiple features.
class AppPlaceholder extends StatelessWidget {
  const AppPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
